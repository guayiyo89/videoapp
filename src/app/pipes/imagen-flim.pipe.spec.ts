import { ImagenFlimPipe } from './imagen-flim.pipe';

describe('ImagenFlimPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenFlimPipe();
    expect(pipe).toBeTruthy();
  });
});
