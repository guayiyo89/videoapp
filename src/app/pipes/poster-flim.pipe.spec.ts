import { PosterFlimPipe } from './poster-flim.pipe';

describe('PosterFlimPipe', () => {
  it('create an instance', () => {
    const pipe = new PosterFlimPipe();
    expect(pipe).toBeTruthy();
  });
});
